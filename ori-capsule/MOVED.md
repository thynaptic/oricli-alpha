# Moved → Keep

This tree has been extracted to its own repository:

**https://github.com/genoventures-labs/Keep**

Working name: **Keep** (not ORI-branded).

In this monorepo it will live as the `keep/` git submodule.  
See `keep/README.md` and Keep’s `ORIGIN.md`.
